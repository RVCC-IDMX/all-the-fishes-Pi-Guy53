class bubbleColumn {

    bubbleColumn(spriteNum, speed, decayTime) {
        this.spriteNum = spriteNum;
        this.speed = speed;
        this.decayTime = decayTime;
    }

    start() {
        let particles = [];
        for (let i = 0; i < this.spriteNum; i++)
    }

}